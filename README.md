# stick_app
Application for automatically generating embroidery patterns from image files

Accepted input formats: jpeg

Output formats: jpeg, pdf

Programming language: Python

Required libraries (desktop version): tkinter, PIL, numpy, scikit-learn, torch, torchvision, reportlab

Required libraries (Android version): kivy, PIL, numpy, scikit-learn, torch, torchvision, reportlab
